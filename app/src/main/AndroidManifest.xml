<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.informatika.myexpensetrackeroka"> <!-- Sesuaikan package name -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyExpenseTracker"
        tools:targetApi="31">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AddExpenseActivity"
            android:label="@string/tambah_pengeluaran"
            android:parentActivityName=".activity.MainActivity"/>
        <activity android:name=".activity.EditExpenseActivity"
            android:label="@string/edit_pengeluaran"
            android:parentActivityName=".activity.MainActivity"/>
    </application>

</manifest>